// подключение материал ui
npm install @material-ui/core

//Подключение шрифта для материал ui
npm install fontsource-roboto , Затем подключить через импорт import 'fontsource-roboto';

//подкл иконочного шрифта
npm install @material-ui/icons
для работы:
import AccessAlarmIcon from '@material-ui/icons/AccessAlarm';
import ThreeDRotation from '@material-ui/icons/ThreeDRotation';
Второй вариант:
import { AccessAlarm, ThreeDRotation } from '@material-ui/icons';

//SVG
npm install @material-ui/icons

//Выбрать иконку и сделать импорт к компонент

//Подключение: в index.jsx импортируем. Далее обернуть верстку в тег </StylesProvider>
import { StylesProvider, createGenerateClassName } from '@material-ui/core/styles';